simpy>=4.0.0
matplotlib>=3.5.0

# How to Install
# Open your VS Code terminal and run this command to install all dependencies at once:
pip install -r requirements.txt

Libraries Explanation:
simpy: Handles the simulation "clock," the truck process, and the random waste generation events.
matplotlib: Handles the graphical interface, the map drawing, and the real-time animation of the truck.